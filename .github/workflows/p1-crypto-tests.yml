name: P1-01 Secure Random Tests

on:
  push:
    paths:
      - 'host/c/crypto_builtin.c'
      - 'host/c/tllvm.h'
      - 'host/c/builtin.c'
      - 'compiler/codegen.tll'
      - 'compiler/linker.tll'
      - 'tests/crypto/**'
      - '.github/workflows/p1-crypto-tests.yml'
  pull_request:
    paths:
      - 'host/c/crypto_builtin.c'
      - 'tests/crypto/**'

jobs:
  test-linux:
    name: Linux (Ubuntu 22.04)
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: sudo apt-get update && sudo apt-get install -y gcc make

      - name: Check environment
        run: |
          gcc --version
          free -h
          df -h
          echo "=== File checks ==="
          ls -la host/c/*.c
          echo "=== sqlite3.c first 5 lines ==="
          head -5 host/c/sqlite3.c
          echo "=== sqlite3.c size ==="
          wc -c host/c/sqlite3.c
          echo "=== crypto_builtin.c first 5 lines ==="
          head -5 host/c/crypto_builtin.c
          echo "=== Check for CRLF ==="
          file host/c/*.c | head -10

      - name: Build tllvm
        run: |
          cd host/c
          echo "=== Files in host/c ===" > build_log.txt
          ls -la *.c >> build_log.txt 2>&1
          echo "=== Starting build ===" >> build_log.txt
          gcc -O0 -w -std=c99 -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE -o tllvm main.c vm.c value.c json.c builtin.c sqlite_builtin.c crypto_builtin.c sqlite3.c -lm -lpthread -ldl >> build_log.txt 2>&1
          BUILD_EXIT=$?
          echo "=== Build exit code: $BUILD_EXIT ===" >> build_log.txt
          cat build_log.txt
          if [ $BUILD_EXIT -ne 0 ]; then
            echo "::error::Build failed with exit code $BUILD_EXIT"
            echo "::error::=== Build log ==="
            while IFS= read -r line; do
              echo "::error::$line"
            done < build_log.txt
            exit 1
          fi
          ls -la tllvm

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: linux-build-log
          path: host/c/build_log.txt
          retention-days: 7

      - name: Build TLL compiler
        run: |
          ./host/c/tllvm tools/TLLC/tllc.tllbc compile tools/TLLC/main.tll -o tools/TLLC/tllc_new.tllbc
          ls -la tools/TLLC/tllc_new.tllbc

      - name: Run Secure Random Gate tests
        run: |
          ./host/c/tllvm tools/TLLC/tllc_new.tllbc compile tests/crypto/gate_secure_random.tll -o tests/crypto/gate_secure_random.tllbc
          ./host/c/tllvm tests/crypto/gate_secure_random.tllbc

      - name: Run multi-thread tests
        run: |
          ./host/c/tllvm tools/TLLC/tllc_new.tllbc compile tests/crypto/test_multithread.tll -o tests/crypto/test_multithread.tllbc
          ./host/c/tllvm tests/crypto/test_multithread.tllbc

  test-windows:
    name: Windows (MSVC)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Build tllvm.exe
        shell: cmd
        run: |
          cd host\c
          cl /O2 /D_CRT_SECURE_NO_WARNINGS /D_WIN32 /Fe:tllvm.exe main.c vm.c value.c json.c builtin.c sqlite_builtin.c crypto_builtin.c sqlite3.c /link bcrypt.lib advapi32.lib
          if errorlevel 1 exit /b 1
          dir tllvm.exe

      - name: Build TLL compiler
        shell: cmd
        run: |
          .\host\c\tllvm.exe tools\TLLC\tllc.tllbc compile tools\TLLC\main.tll -o tools\TLLC\tllc_new.tllbc
          if errorlevel 1 exit /b 1

      - name: Run Secure Random Gate tests
        shell: cmd
        run: |
          .\host\c\tllvm.exe tools\TLLC\tllc_new.tllbc compile tests\crypto\gate_secure_random.tll -o tests\crypto\gate.tllbc
          if errorlevel 1 exit /b 1
          .\host\c\tllvm.exe tests\crypto\gate.tllbc
          if errorlevel 1 exit /b 1

      - name: Run multi-thread tests
        shell: cmd
        run: |
          .\host\c\tllvm.exe tools\TLLC\tllc_new.tllbc compile tests\crypto\test_multithread.tll -o tests\crypto\mt.tllbc
          if errorlevel 1 exit /b 1
          .\host\c\tllvm.exe tests\crypto\mt.tllbc
          if errorlevel 1 exit /b 1

  test-macos:
    name: macOS (Apple Silicon / Intel)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build tllvm
        run: |
          cd host/c
          clang -O2 -std=c99 -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE -o tllvm main.c vm.c value.c json.c builtin.c sqlite_builtin.c crypto_builtin.c sqlite3.c -lm -lpthread
          ls -la tllvm

      - name: Build TLL compiler
        run: |
          ./host/c/tllvm tools/TLLC/tllc.tllbc compile tools/TLLC/main.tll -o tools/TLLC/tllc_new.tllbc
          ls -la tools/TLLC/tllc_new.tllbc

      - name: Run Secure Random Gate tests
        run: |
          ./host/c/tllvm tools/TLLC/tllc_new.tllbc compile tests/crypto/gate_secure_random.tll -o tests/crypto/gate_secure_random.tllbc
          ./host/c/tllvm tests/crypto/gate_secure_random.tllbc

      - name: Run multi-thread tests
        run: |
          ./host/c/tllvm tools/TLLC/tllc_new.tllbc compile tests/crypto/test_multithread.tll -o tests/crypto/test_multithread.tllbc
          ./host/c/tllvm tests/crypto/test_multithread.tllbc
