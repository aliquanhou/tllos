# P0-15.17 施工报告：4 Node Real Blockchain Network

## 当前状态：核心能力已验证，进入收尾阶段

### 一、已完成的关键里程碑

#### 1. 4 节点独立进程区块链网络 (commit e31902e)
- **4 个独立 TLL 进程**，每个拥有独立的 VM、Scheduler、Coroutine、TCP socket、Chain、Mempool
- **星型拓扑**：Node A (leader, port 19101) + Node B/C/D (followers, ports 19102-19104)
- **真实 TCP P2P 通信**，无 Mock、无共享内存
- **验证结果**：4 节点达成链共识（height=1, 相同 tip hash, all valid=true）

#### 2. hexToInt TCP 粘包解码 Bug 修复 (commit a59763b)
- **根因**：`hexToInt()` 使用 `convert.toInt()` 解析 a-f 字符，但 `convert.toInt()` 对非数字字符串返回 0，导致所有 a-f 十六进制位都被错误解析为 10（0-0+10）
- **影响**：当消息长度前缀包含 a-f 字符时（如 0x023c 被解析为 570 而非 572），消息长度解析错误，导致 TCP 粘包解码错位，后续消息全部无法解析
- **修复**：将 `convert.toInt()` 算术替换为 0-9a-fA-F 的显式字符比较
- **验证**：修复后 5 区块连续同步全部成功，无 "No handler for message type" 错误

#### 3. 多区块连续同步测试 (commit a59763b)
- Node A 连续挖出 **5 个区块**，每个区块包含真实交易
- 区块间隔 2 秒，确保传播完成
- **验证结果**：全部 4 节点同步到 height=5，tip hash 完全一致（0bd033...），all valid=true
- 证明：交易传播 → 区块广播 → 链验证 → 链同步 完整闭环

### 二、关键技术修复

#### 1. p2p.tll 消息处理器上下文传递
- **问题**：原 `p2pProcessPeerData` 调用 `handler(msg, peer)`，但 blockchain 节点对象存储在 `p2p._bcNode` 上，不在 peer 上，导致消息处理器无法访问 blockchain 节点
- **修复**：改为 `handler(msg, peer, node)`，传递 p2p 节点对象，处理器通过 `p2pNode._bcNode` 获取 blockchain 节点
- **兼容性**：现有处理器忽略多余参数，不破坏兼容性

#### 2. blockchain.tll merkleRoot 清理
- 移除了基于误判的 VM Bug workaround（`while _ci < 10000` 计数）
- 移除了大量 `io.println("[DEBUG merkleRoot]...")` 调试输出
- 替换为简洁正确的实现

#### 3. blockchain_node.tll 整合模块
- 新建 `stdlib/blockchain_node.tll`，整合 blockchain + mempool + p2p
- 实现完整节点逻辑：节点创建/启动/停止、交易提交/验证/广播/中继、区块挖矿/验证/接入链/广播、链同步（get_blocks/blocks 消息）、ping/pong、节点状态查询

### 三、测试验证矩阵

| 测试场景 | 状态 | 验证内容 |
|---------|------|---------|
| 4 Node 基础网络 | ✅ PASS | 4 独立进程 + 真实 TCP + 链共识 |
| 交易传播 | ✅ PASS | Node A 创建交易 → B/C/D 收到并验证 |
| 区块广播 | ✅ PASS | Node A 挖矿 → B/C/D 收到并验证区块 |
| 多区块同步 (5 blocks) | ✅ PASS | 连续 5 区块，全部节点同步一致 |
| 链验证 | ✅ PASS | 所有节点 valid=true |
| TCP Framing (粘包) | ✅ PASS | hexToInt 修复后多消息正确解码 |
| Coroutine 生命周期 | ✅ PASS | 基础测试无回归 |
| Channel | ✅ PASS | 基础测试无回归 |
| IO-aware Scheduler | ✅ PASS | 基础测试无回归 |

### 四、提交记录

| Commit | 说明 |
|--------|------|
| 8343dc9 | P0-15.16 Runtime 验收封板 |
| 7e3476b | json.c 常量池引用计数修复 |
| e31902e | 4 节点基础区块链网络 + p2p 上下文修复 |
| a59763b | hexToInt TCP 粘包修复 + 多区块同步测试 |

### 五、待完成项（后续迭代）

1. **节点掉线/重连测试**：Node C kill → 其他节点继续 → Node C restart → 重新连接 → 恢复同步
2. **非法块拒绝测试**：构造错误 previousHash/PoW/index 的区块，验证其他节点拒绝
3. **交易去重测试**：验证重复交易不会无限传播
4. **多节点挖矿测试**：不同节点轮流产生区块
5. **CI 集成**：将 4 节点测试集成到 GitHub Actions

### 六、核心结论

**P0-15.17 的核心目标已经达成**：4 个独立 TLL 进程通过 TLL Runtime 的真实 TCP + Coroutine + Scheduler 建立了真实 P2P 区块链网络，完成了交易传播 → 区块广播 → 链验证 → 链同步的完整闭环。

这是 TLL 语言的重要里程碑：**TLL 自己写出来的区块链代码，能够在 TLL Runtime 上作为真实多进程网络运行**。

### 七、文件清单

**新增文件：**
- `stdlib/blockchain_node.tll` — 区块链节点整合模块
- `tests/bc_node_a.tll` ~ `tests/bc_node_d.tll` — 4 节点基础测试
- `tests/run_4node_bc_test.ps1` — 4 节点测试驱动
- `tests/bc_multi_a.tll` ~ `tests/bc_multi_d.tll` — 多区块同步测试
- `tests/run_multi_block_test.ps1` — 多区块测试驱动

**修改文件：**
- `stdlib/p2p.tll` — hexToInt 修复 + 消息处理器上下文传递
- `stdlib/blockchain.tll` — merkleRoot 清理
