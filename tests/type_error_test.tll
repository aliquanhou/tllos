// Type checking test - should produce type warnings
fn add(a: int, b: int) -> int {
    return a + b
}

fn main() {
    // This should trigger type warning: string passed to int parameter
    let result = add("hello", 10)
    io.println(result)
}
