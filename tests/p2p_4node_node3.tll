// P0-15.17: 4-Node test - Peer Node3
from "../../stdlib/p2p" import createP2PNode, p2pConnect, p2pStart, p2pStop, p2pSendToPeer, p2pBroadcast, p2pRegisterHandler, p2pPeerCount

let g_announceCount = 0
let peer = null

fn onAnnounce(msg, peerRef) {
    g_announceCount = g_announceCount + 1
    io.println("[node3] Received ANNOUNCE from " + peerRef.nodeId + " seq=" + convert.toString(msg.payload.seq) + " text=" + msg.payload.text)
    p2pSendToPeer(peer, 0, "ack", { src: "node3", seq: msg.payload.seq })
    io.println("[node3] Sent ACK reply")
}

peer = createP2PNode("node3", "127.0.0.1", 19033)
p2pRegisterHandler(peer, "announce", onAnnounce)

io.println("=== Node3 Starting ===")
io.println("[node3] Connecting to leader...")
let ok = p2pConnect(peer, "127.0.0.1", 19031)
io.println("[node3] Connect result: " + convert.toString(ok))
if !ok {
    io.println("[node3] FAILED to connect")
    return
}

p2pStart(peer)
io.println("[node3] Started, peers=" + convert.toString(p2pPeerCount(peer)))

io.println("[node3] Sending HELLO to leader...")
p2pSendToPeer(peer, 0, "hello", { src: "node3", seq: 1 })

let waitTicks = 0
while waitTicks < 100 {
    coroutine.sleep(100)
    waitTicks = waitTicks + 1
    if g_announceCount > 0 {
        io.println("[node3] Got ANNOUNCE, waiting for ACK to send...")
        coroutine.sleep(500)
        break
    }
}

io.println("[node3] Final: announces=" + convert.toString(g_announceCount) + " peers=" + convert.toString(p2pPeerCount(peer)))
p2pStop(peer)
io.println("=== Node3 Done ===")
